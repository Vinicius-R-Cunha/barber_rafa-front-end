import styled from "styled-components"

const ScheduleDiv = styled.div`

    .schedule-title {
        font-size: 40px;
        font-weight: 700;

        margin-bottom: 40px;
    }

    .weekday {
        font-size: 25px;
        
        display: flex;
        justify-content: space-between;

        margin-bottom: 35px;
    }
`

export { ScheduleDiv }